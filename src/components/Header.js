import React from "react";
import styled from "styled-components";


const replateHeader = styled.p`
    text-align: center;

`

export default function Header() {
    return (
        <replateHeader>Replate</replateHeader>

    )
}