import React from "react";
import styled from "styled-components";


const ReplateHeader = styled.h1`
    text-align: center;
    font-family: 'BalooBhai';
`

export default function Header() {
    return (
        <ReplateHeader>Replate</ReplateHeader>

    )
}